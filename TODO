TODO
====
- Enable all auth mechanisms to work on 1:n passed in connections instead of using the pool.getAll
- Ensure new connections are authenticated if needed
- Closing connections should only affect operations on that connection
  + Add connection to callbackstore callbacks
  + Flush connection callbacks
- What does reconnect mean ?
  + Server
  + ReplSet