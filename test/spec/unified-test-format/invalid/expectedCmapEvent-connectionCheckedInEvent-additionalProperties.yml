description: expectedCmapEvent-connectionCheckedInEvent-additionalProperties

schemaVersion: '1.3'

tests:
  - description: foo
    operations: []
    expectEvents:
      - client: client0
        eventType: cmap
        events:
          - connectionCheckedInEvent:
              foo: bar
