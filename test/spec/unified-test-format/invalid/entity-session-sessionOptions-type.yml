description: "entity-session-sessionOptions-type"

schemaVersion: "1.0"

createEntities:
  - client:
      id: &client0 "client0"
  - session:
      id: &session0 "session0"
      client: *client0
      sessionOptions: 0

tests:
  - description: "foo"
    operations: []
