description: "expectedLogMessage-component-enum"

schemaVersion: "1.13"

createEntities:
  - client:
      id: &client0 "client0"

tests:
  - description: "foo"
    operations: []
    expectLogMessages:
      - client: *client0
        messages:
          - level: debug
            component: foo
            data: {}
